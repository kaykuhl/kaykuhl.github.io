<div class="container">

<div class="row">
<div class="col-md-12"><h1>Eat-Da-Burger</h1></div>  
</div>

<div class="row">
<div class="col-md-6">
<h2>Burgers to be Eaten!</h2>

<ul>
  {{#each burgers}}
    {{!-- {{#unless devoured}} --}}
      <li>
        <p>Name: {{burger_name}}</p>
        {{!-- <button class="devoured btn" data-id="{{id}}" data-devoured="{{devoured}}">EAT!</button>
  	<button class="delete-burger" data-id="{{id}}">DELETE!</button> --}}
      </li>
    {{!-- {{/unless}} --}}
  {{/each}}
</ul>
</div>


<div class="col-md-6">
{{!-- <h2>Devoured Burgers!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
           <li>
        <p>{{name}}</p>
  	<button class="delete-burger" data-id="{{id}}">DELETE!</button>
      </li>
    {{/if}}
  {{/each}}
</ul> --}} 

</div>
</div>

<div class="row">
  <div class="col-md-12">
<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="burger_name">
  </div>

  <button type="submit">Add Burger</button>
</form>
</div>
</div>
</div>
